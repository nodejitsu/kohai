#!/usr/bin/env node

var kohai = require('../lib/kohai'),
    path = require('path'),
    utile = require('utile'),
    port = kohai.config.get('http-port') || 9000;

kohai.start(function (err) {

  if (err) {
    kohai.log.error('there was an error.');
    console.log(err);
  }

});
