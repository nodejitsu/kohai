#!/usr/bin/env node

// Require kohai as a node.js module
// Once it's loaded, execute the start method
var kohai = require('../lib/kohai');

// kohai will now start up and connect to the IRC specified in your config.json file
kohai.start(kohai.getConfig());
